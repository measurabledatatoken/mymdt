import users from './users';
import rewards from './rewards';
import transactions from './transactions';
import applications from './applications';
import rewardHistories from './rewardHistories';

const modules = {
  users,
  rewards,
  transactions,
  applications,
  rewardHistories,
};

export default {
  modules,
};
